{
  "name": "sap-proxy-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Server-side SAP B1 Service Layer proxy. Holds the SAP credentials and pools one Service Layer session for all clients.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "compression": "^1.7.5",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "helmet": "^8.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
